<UserControl xmlns:Proben="clr-namespace:MeisterGeister.View.Proben"  x:Class="MeisterGeister.View.Proben.ProbenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewGeneral="clr-namespace:MeisterGeister.View.General"
             mc:Ignorable="d" 
             d:DesignHeight="451" d:DesignWidth="601"  Background="{StaticResource Background1}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!--<Proben:ProbeControl Margin="5" />-->
            <Proben:ProbeControl Margin="5" DataContext="{Binding ProbeItemListe[0]}" />
            <Proben:ProbeControl Margin="5" />
            <Button Content="Alle Würfeln" Margin="20" Command="{Binding OnWürfeln}" />
        </StackPanel>
        
        <Expander Grid.Column="1" Grid.Row="1" IsExpanded="True">
            <Expander.Header>
                <TextBlock Text="Proben" Style="{DynamicResource TextTitel}" />
            </Expander.Header>
            <Border Style="{DynamicResource ControlContentBorder}">
                <ListBox BorderBrush="#00000000" Background="#00000000" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ItemsSource="{Binding ProbeItemListe}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollContentPresenter}}" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5">
                                <StackPanel>
                                    <Proben:ProbeControl DataContext="{Binding}" />
                                    <TextBlock Text="{Binding Held}" />
                                    <TextBlock Text="{Binding Ergebnis.Übrig}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Expander>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="1" Margin="10" VerticalAlignment="Top">
            <TextBlock Text=""/>
            <TextBlock Text="Proben" Margin="5,0,10,0" />
            <TextBlock Text="" ToolTip=""/>
        </StackPanel>
        
    </Grid>
</UserControl>
