<UserControl xmlns:Proben="clr-namespace:MeisterGeister.View.Proben"  x:Class="MeisterGeister.View.Proben.ProbenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="320" d:DesignWidth="408"  Background="{StaticResource Background1}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="74*"/>
            <RowDefinition Height="133*"/>
            <RowDefinition Height="93*"/>
        </Grid.RowDefinitions>
        <Expander Grid.Column="1" Grid.Row="1" IsExpanded="True">
            <Expander.Header>
                <TextBlock Text="Proben" Style="{DynamicResource TextTitel}" />
            </Expander.Header>
            <Border Style="{DynamicResource ControlContentBorder}">
                <WrapPanel>
                    <Border Margin="5" Style="{DynamicResource ControlContentBorder}">
                        <Proben:ProbeControl/>
                    </Border>
                    <Border Margin="5" Style="{DynamicResource ControlContentBorder}">
                        <Proben:ProbeControl/>
                    </Border>
                    <Border Margin="5" Style="{DynamicResource ControlContentBorder}">
                        <Proben:ProbeControl/>
                    </Border>
                </WrapPanel>
            </Border>
        </Expander>
    </Grid>
</UserControl>
