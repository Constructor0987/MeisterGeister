<StackPanel  x:Name="btn" x:Class="MeisterGeister.View.AudioPlayer.btnHotkey"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:MeisterGeister.ViewModel.AudioPlayer.Logic"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Button Background="LightGray" Margin="5,0" Height="25" Width="25" 
            Content="{Binding tasteChar}"
            Command="{Binding OnBtn}"
            ToolTip="{Binding aPlaylist.Name}" />
</StackPanel>