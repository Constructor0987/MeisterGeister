<UserControl x:Class="MeisterGeister.View.Generator.GeneratorPersonNurNameControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border Margin="5" Padding="4" CornerRadius="5">
        <Border.Background>
            <LinearGradientBrush EndPoint="1,0.5" Opacity="0.3" StartPoint="0,0.5">
                <GradientStop Color="WhiteSmoke" Offset="0" />
                <GradientStop Color="#FFFFFFC7" Offset="1" />
            </LinearGradientBrush>
        </Border.Background>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBlock FontWeight="Bold" Text="{Binding Name}" />
                <TextBlock FontSize="9" Text="{Binding Geschlecht}" />
            </StackPanel>
            <TextBlock FontSize="9" Text="{Binding Namensbedeutung}" />
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="11" FontWeight="Bold" Text="Namenstyp: " />
                <TextBlock FontSize="11" Text="{Binding Namenstyp}" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
